1. Explain the difference between the == operator and the === operator.
=== kijkt of waarden precies identiek zijn. Bij == worden automatisch typen geconverteerd

2. Explain what a closure is. (Note that JavaScript programs use closures very often.)
Door closure wordt een global scope variabele lokaal

3. Explain what higher order functions are.
Deze functies nemen een of meerdere functies als argument en/of returnen een functie

4. Explain what a query selector is and give an example line of JavaScript that uses a query selector.
Een query selector return het eerste element in een document dat overeenkomt met de specifier

    element = document.querySelector(selectors);